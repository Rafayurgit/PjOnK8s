apiVersion: v1
kind: Service
metadata:
  name: photojugaad-frontend-service
spec:
  type: ClusterIP
  selector:
    app: photojugaad-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081